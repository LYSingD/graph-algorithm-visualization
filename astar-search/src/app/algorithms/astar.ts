import { TupleType } from 'typescript';

// More A* Search algorithm details could be found in: https://en.wikipedia.org/wiki/A*_search_algorithm
export default function AStarSearch(maze_grid, starter, target) {
  let path: Array<TupleType> = [];
  return path;
}
